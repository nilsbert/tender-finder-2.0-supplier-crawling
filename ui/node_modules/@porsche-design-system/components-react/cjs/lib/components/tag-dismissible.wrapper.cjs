"use client";
'use strict';

var jsxRuntime = require('react/jsx-runtime');
var react = require('react');
var hooks = require('../../hooks.cjs');
var utils = require('../../utils.cjs');

const PTagDismissible = /*#__PURE__*/ react.forwardRef(({ aria, compact = false, label, className, ...rest }, ref) => {
    const elementRef = react.useRef(undefined);
    const WebComponentTag = hooks.usePrefix('p-tag-dismissible');
    const propsToSync = [aria, compact, label];
    hooks.useBrowserLayoutEffect(() => {
        const { current } = elementRef;
        ['aria', 'compact', 'label'].forEach((propName, i) => (current[propName] = propsToSync[i]));
    }, propsToSync);
    const props = {
        ...rest,
        class: hooks.useMergedClass(elementRef, className),
        ref: utils.syncRef(elementRef, ref)
    };
    // @ts-ignore
    return jsxRuntime.jsx(WebComponentTag, { ...props });
});

exports.PTagDismissible = PTagDismissible;
